apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: yaslog-gateway
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "*"

---

apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: publication-virtsrv
spec:
  hosts:
    - "*"
  gateways:
    - yaslog-gateway
  http:
    - match:
        - uri:
            prefix: "/api/auth"

      route:
        - destination:
            port:
              number: 80
            host: publication-service.default.svc.cluster.local

---

apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: django-virtsrv
spec:
  hosts:
    - "*"
  gateways:
    - yaslog-gateway
  http:
    - match:
        - uri:
            prefix: "/api/auth"
      route:
        - destination:
            port:
              number: 80
            host: auth-service.default.svc.cluster.local